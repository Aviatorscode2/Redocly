name: mode
in: query
description: >-
  **Mode**. *Example: html*. Determines the format of the response. Possible
  values are `json`, `xml`, and `html`. If the mode parameter is empty, the
  format is `json` by default.
schema:
  type: string
  enum:
    - json
    - xml
    - html
  default: json
