name: zip
in: query
description: >-
  **Zip code**. Search by zip code. *Example: 95050,us*. Please note that if the
  country is not specified, the search uses USA as a default.
schema:
  type: string
